name: data
description: Data module
publish_to: 'none'
version: 0.0.1

environment:
  sdk: '>=3.1.5 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  dependencies:
    path: ../common/dependencies

  domain:
    path: ../domain
  utils:
    path: ../libs/utils




dev_dependencies:
  flutter_test:
    sdk: flutter
  dev_dependencies:
    path: ../common/dev_dependencies

