name: domain
description: Domain module
publish_to: 'none'
version: 1.0.0

environment:
  sdk: '>=3.1.5 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  dependencies:
    path: ../common/dependencies

  utils:
    path: ../libs/utils


dev_dependencies:
  flutter_test:
    sdk: flutter
  dev_dependencies:
    path: ../common/dev_dependencies
