name: domain
description: Domain module
publish_to: 'none'
version: 1.0.0

environment:
  sdk: '>=3.1.5 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  utils:
    path: ../libs/utils

  uuid: ^4.1.0
  collection: ^1.18.0

  firebase_auth: ^4.12.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
